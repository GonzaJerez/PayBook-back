import { CheckCategoryActiveGuard } from './check-category-active.guard';

describe('CheckCategoryActiveGuard', () => {
  it('should be defined', () => {
    expect(new CheckCategoryActiveGuard()).toBeDefined();
  });
});
